// Shim: the shared domain types moved to the @polycode/shared workspace
// package (packages/shared) so the mobile app can consume them too.
// This re-export keeps every existing relative import working unchanged.
export * from '@polycode/shared'
